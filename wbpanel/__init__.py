# -*- coding: utf-8 -*-
"""
wb-svg-panel - SVG-панели умного дома для контроллеров Wiren Board.

Один процесс делает всё:
  * держит постоянную подписку на /devices/+/controls/+ и хранит состояние
    в памяти;
  * фоновым потоком тянет историю для графиков через MQTT-RPC или напрямую
    из SQLite;
  * отдаёт панели по HTTP как image/svg+xml.

Вся визуальная часть живёт в templates/ - Python сюда не лезет, он только
считает числа и геометрию.

Раскладка пакета:

    const     пути, размеры сетки, постоянные величины
    config    чтение config.yaml
    state     состояние MQTT в памяти, MQTT-RPC
    history   история значений для графиков
    geometry  цвета, шкалы, сглаживание, раскладка по сетке
    tiles     сборка данных для каждого типа плитки
    status    строка состояния комнаты
    assemble  сборка панели целиком
    render    подстановка CSS-переменных
    web       HTTP-маршруты и точка входа
"""

__version__ = "0.9.1"
